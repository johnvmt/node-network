var assert = require('assert');
var nodeNetwork = require('../')();

describe('OMS Doc Find', function(){
	describe('Database doc', function(){
		it('should insert a doc, then return it', function(done) {

			done();
		});
	});
});